\new TabStaff {
  \relative c' {
      % chord hammer-on and pull-off
          \once \set doubleSlurs = ##t
	      <g' b>8( <a c> <g b>)
	          g( a2) % to check that slur is single again
		    }
		    }

